# Todos App using Next 13
This is a simple todo app using Next 13. Styled using CSS. 

## Learnings
- How to add server actions in client components
  Use `useTransition` hook from react to update serverActions in client component

- Better understanding on TypeScript 
  `String` is different from `string`

## Fixes Needed
- Hardcoded the API as of now, can do a conditional check for local environment or prod and update URL from .env file accordingly